var Component = require("montage/ui/component").Component;

/**
 * @class OverlayComponent
 * @extends Component
 */
var OverlayComponent = exports.OverlayComponent = Component.specialize(/** @lends OverlayComponent.prototype */ {

    constructor: {
        value: function OverlayComponent() {
            if (this.constructor === OverlayComponent) {
                throw new Error("OverlayComponent cannot be instantiated.");
            }
        }
    },

    hasTemplate: {
        value: false
    },

    didZoom: {
        value: function () {}
    },

    redraw: {
        value: function () {}
    },

    willZoom: {
        value: function () {}
    }

});
