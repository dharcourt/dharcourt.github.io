var Montage = require("montage").Montage,
    ObjectDescriptor = require("montage-data/logic/model/object-descriptor").ObjectDescriptor;

/**
 * Provides authorization. Defined by the <AuthService> elements in the XML
 * configuration file.
 *
 * @class Authority
 * @extends external:Montage
 */
exports.Authority = Montage.specialize(/** @lends Authority# */ {

    // These objects have no data for now.

}, {

    /**
     * The Montage Data type of authorities.
     *
     * @type {external:ObjectDescriptor}
     */
    TYPE: {
        get: ObjectDescriptor.getterFor(exports, "Authority")
    }

});
