/**
 * @module logic/hazard-selector.js
 */
var ObjectSelector = require("logic/mock-montage-data/object-selector").ObjectSelector;

/**
 * @class HazardSelector
 * @extends ObjectSelector
 */
exports.HazardSelector = ObjectSelector.specialize(/** @lends HazardSelector.prototype */ {

    /**
     * HazardSelector is a selector used to access the mock hazard service.
     */
    constructor: {
        value: function HazardSelector(layer,filter) {
            this.super("hazard");
            this.layer = layer;
            this.filter = filter ? filter : {sort:{}};
        }
    }
});
