/**
 * @module logic/social-selector.js
 */
var ObjectSelector = require("logic/mock-montage-data/object-selector").ObjectSelector;

/**
 * @class TrafficCameraSelector
 * @extends ObjectSelector
 *
 */
exports.TrafficCameraSelector = ObjectSelector.specialize(/** @lends TileSelector.prototype */ {
    constructor: {
        value: function TrafficCameraSelector(layer, extent, filter) {
            this.super("trafficCamera");
            this.layer = layer;
            this.extent = extent;
            this.filter = filter ? filter : {sort: {}};
        }
    }
});
