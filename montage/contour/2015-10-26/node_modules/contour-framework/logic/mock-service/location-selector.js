/**
 * @module logic/feature-selector.js
 */
var ObjectSelector = require("logic/mock-montage-data/object-selector").ObjectSelector;

/**
 * @class LocationSelector
 * @extends ObjectSelector
 */
exports.LocationSelector = ObjectSelector.specialize(/** @lends LocationSelector.prototype */ {

    constructor: {
        value: function LocationSelector(searchText) {
            this.super("location");
            this.searchText = searchText;
        }
    }
});
