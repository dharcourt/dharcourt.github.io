/**
 * @module logic/feature-selector.js
 */
var ObjectSelector = require("logic/mock-montage-data/object-selector").ObjectSelector;

/**
 * @class BookmarkSelector
 * @extends ObjectSelector
 */
exports.BookmarkSelector = ObjectSelector.specialize(/** @lends BookmarkSelector.prototype */ {

    constructor: {
        value: function BookmarkSelector(bookmarkId) {
            this.super("bookmark");
            if (bookmarkId) {
                this.bookmarkId = bookmarkId;
            }
        }
    }
});
