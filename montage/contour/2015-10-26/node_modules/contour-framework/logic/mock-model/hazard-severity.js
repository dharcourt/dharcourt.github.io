var Montage = require("montage").Montage,
    Enum = require("montage/core/enum").Enum;

/**
 * @class HazardSeverity
 * @extends Montage
 *
 * Object representing a hazard severity.
 *
 */
var HazardSeverity = exports.HazardSeverity = Montage.specialize(/** @lends HazardSeverity.prototype */ {

    constructor: {
        value: function HazardSeverity(id, name, icon, color) {
            this.id = id;
            this.icon = icon;
            this.name = name;
            this.color = color;
        }
    },
    id: {
        value: null
    },
    icon: {
        value: null
    },
    color: {
        value:null
    }
});
